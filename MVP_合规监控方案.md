# PDD 商品监控与提醒工具（合规版）MVP 设计文档

> 目标：在不做自动下单、不绕过安全机制的前提下，实现“百亿补贴商品识别 + 可购买状态监控 + 秒级提醒”。

## 1. 项目定位与边界

### 1.1 项目定位
- 这是一个 **监控与提醒系统**，不是自动抢购器。
- 用于提升你手动下单的时效，核心收益来自：
  - 更快发现“百亿补贴”目标商品
  - 更快感知“无货 -> 可买”的状态切换
  - 更快完成“收到通知 -> 打开链接 -> 手动下单”流程

### 1.2 合规边界（必须遵守）
- 不做自动登录、自动下单、自动支付。
- 不做验证码绕过、设备指纹伪造、风控绕过。
- 不做异常高频请求；使用可控、温和轮询策略。
- 不采集与本目标无关的用户隐私数据。

---

## 2. MVP 成功标准

### 2.1 功能性指标
- 能维护目标商品列表（链接 + 关键词 + 价格阈值 + SKU偏好）。
- 能识别“百亿补贴”信号，并输出可信度评分（0~100）。
- 能判断“不可买/可买”状态，并记录状态变化时间。
- 状态变为“可买”时，通知在 3 秒内发出（服务端处理延迟目标）。

### 2.2 质量指标
- 误报率可控（例如每天误报 <= 5 次，后续通过规则优化）。
- 连续运行稳定（7x12 小时不崩溃，异常自动恢复）。
- 所有告警可追溯（日志带商品ID、时间、判定依据）。

---

## 3. 系统架构（MVP）

```text
[配置文件/管理页]
      |
      v
[调度器 Scheduler] ---> [采集器 Collector] ---> [解析器 Parser]
                                              |
                                              v
                                       [规则引擎 Rule Engine]
                                              |
                +-----------------------------+-----------------------------+
                |                                                           |
                v                                                           v
         [状态存储 SQLite/Postgres]                                 [通知服务 Notifier]
                |                                                           |
                v                                                           v
         [日志/审计 Trace]                                             微信/Telegram/邮件
```

### 3.1 模块说明
1. **Scheduler**
   - 定时触发检测任务（默认 30~60 秒，支持抖动随机 5~10 秒）。
2. **Collector**
   - 采集目标商品页面或可用接口返回内容。
3. **Parser**
   - 提取标题、价格、活动文案、按钮文本、SKU 信息。
4. **Rule Engine**
   - 计算“百亿补贴可信度”和“库存/可购买状态”。
5. **Storage**
   - 保存商品配置、历史状态、最近告警记录。
6. **Notifier**
   - 状态切换（不可买 -> 可买）或价格命中时推送。

---

## 4. 关键判定策略

### 4.1 百亿补贴识别（多信号评分）
建议评分模型（示例）：
- 文案包含“百亿补贴”：+40
- 活动标签字段命中：+30
- 展示“补贴价 + 划线价”结构：+15
- 价格落入预设合理区间：+10
- 店铺/商品可信信号（销量、评分阈值）：+5

判定：
- `score >= 80`：高可信百亿补贴
- `60 <= score < 80`：中可信（仅记录或低优先提醒）
- `<60`：不作为目标

### 4.2 库存/可购买状态识别（状态机）
定义状态：
- `UNAVAILABLE`：无货/已抢光/不可购买
- `AVAILABLE`：立即购买/马上抢/可加入购买流程
- `UNKNOWN`：解析失败或页面异常

状态机规则：
- 仅当 `UNAVAILABLE -> AVAILABLE` 时触发“高优先告警”。
- `UNKNOWN` 连续超过 N 次（如 3 次）触发“采集异常告警”。

### 4.3 去重与防抖
- 同一商品 `AVAILABLE` 状态 10 分钟内只发 1 次高优先通知。
- 若价格、SKU、活动标签出现重大变化，可允许再次通知。

---

## 5. 数据模型（MVP）

## 5.1 watch_items（监控项）
- `id`
- `product_url`
- `keyword`（如 iPhone 17 Pro）
- `target_price_min`
- `target_price_max`
- `preferred_sku`（如 256G 黑色）
- `enabled`
- `created_at`, `updated_at`

## 5.2 item_snapshots（快照）
- `id`
- `watch_item_id`
- `captured_at`
- `title`
- `price`
- `promotion_text`
- `buy_button_text`
- `availability_state`（UNAVAILABLE/AVAILABLE/UNKNOWN）
- `subsidy_score`（0~100）
- `raw_hash`（用于去重）

## 5.3 alert_events（告警）
- `id`
- `watch_item_id`
- `trigger_type`（AVAILABLE_TRANSITION / PRICE_MATCH / ERROR）
- `trigger_at`
- `message`
- `notifier_channel`
- `send_status`

---

## 6. 通知策略

### 6.1 通知内容模板（高优先）
- 标题：`[PDD监控] 发现可购买目标商品`
- 内容：
  - 商品名
  - 当前价格（含阈值对比）
  - 百亿补贴评分
  - 当前可买状态
  - 直达链接
  - 触发时间

### 6.2 通道建议
优先级：
1. 企业微信机器人（延迟低、可群通知）
2. Telegram Bot（个人场景易用）
3. 邮件（兜底）

---

## 7. 迭代计划

### Phase 1（1~2 天）
- 建项目骨架、配置管理、单商品监控、控制台日志。

### Phase 2（2~3 天）
- 多商品并发调度、评分规则、状态机、通知接入。

### Phase 3（1~2 天）
- 简单 Web 面板（查看状态与告警历史）、异常重试、去重防抖。

### Phase 4（持续优化）
- 规则参数化（在线调权重）
- 误报回放分析
- 更细粒度 SKU 判定

---

## 8. 技术选型建议

### 方案A（你若偏 Python）
- FastAPI + APScheduler + httpx + SQLite/Postgres
- 优点：开发快、生态成熟

### 方案B（你若偏 Node）
- NestJS/Express + node-cron + axios + SQLite/Postgres
- 优点：前后端协同方便

建议：MVP 首版优先 Python，迭代速度更快。

---

## 9. 风险与应对

- **页面结构变化导致解析失效**
  - 应对：解析器多策略 + 失败告警 + 快速热修规则。
- **请求过频触发限制**
  - 应对：降频、随机抖动、指数退避、分组轮询。
- **误报影响体验**
  - 应对：评分阈值调优、二次确认规则、告警去重。

---

## 10. 你现在就能执行的落地清单

1. 先准备 3~5 个真实目标链接（含不同规格 SKU）。
2. 定义你的“命中区间”：
   - 目标价格区间
   - 必要关键词（如“百亿补贴”）
   - 可接受 SKU（颜色/容量）
3. 先接 1 个通知通道（建议企业微信或 Telegram）。
4. 上线后观察 2~3 天，基于误报样本调阈值。

---

## 11. 下一步（我建议）

如果你确认，我们下一步直接进入“可编码规格（SRS）”并给出：
- 目录结构
- API 定义
- 核心伪代码（调度、判定、去重、通知）
- `.env` 配置模板
- 本地启动步骤

这样你可以当天就开工写第一版。
